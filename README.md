# Candles_Soap

Сайт-каталог изделий ручной работы Анны Манасарян: свечи, мыло, гипс, декор.
Прототип для показа заказчику — кликабельный сайт, не макет.

## Стек

Next.js 15 (App Router, TypeScript) · Tailwind CSS v4 · Framer Motion · Lenis · next/image.
CMS нет: контент лежит в `src/data/*.json` и разбирается zod-схемами.

## Запуск

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # продакшен-сборка, все страницы пререндерятся статически
npm run typecheck
```

## Структура

```
src/data/          контент: categories, tags, products, site
src/lib/schemas.ts zod-схемы — единственное описание формы данных
src/lib/content.ts единственная точка чтения контента
src/components/    ui/ — переиспользуемое, home/ — блоки главной
tools/ingest.mjs   обработка изображений: старый сайт или локальные папки
tools/photo-plan.json  разбивка кадров по товарам и тексты карточек
tools/photo-metrics.mjs  резкость, дубли и ориентация оригиналов
```

### Замена источника данных на REST

Компоненты не импортируют JSON напрямую — только функции из `src/lib/content.ts`.
Чтобы перейти на API, переписывается один этот модуль: схемы и типы остаются,
ни один компонент не меняется.

## Модель данных

- `categories.json` — 8 категорий: `candles`, `soap`, `gypsum`, `sachet`,
  `holders`, `boxes`, `plates`, `decor`.
- `tags.json` — теги трёх групп: `occasion` (повод), `recipient` (адресат)
  и `form` (форма).
- `products.json` — у товара ровно одна категория и сколько угодно тегов.

Плоский список из 38 разделов заказчицы раскладывается на эти оси: то, что было
отдельным разделом («новогодние свечи», «мыло для мужчин»), собирается фильтрами
категория + теги. Внутри группы теги складываются по «или», между группами —
по «и». `form` пока состоит из одного тега `geometric` и проставляется только
свечам и мылу строгой геометрической формы: у гипса форма и есть сам товар,
фильтровать по ней нечего.

У каждого товара есть `article` — по нему заказчица находит изделие, когда о нём
спрашивают. Префикс задаётся категорией (СВ, МЛ, ГП, СШ, ПС, ШК, ПД, ДК), номер
идёт по порядку внутри категории.

`price` у всех товаров `null` и таким остаётся: заказчица просила убрать цены
с сайта совсем. Поле в схеме сохранено — если решение изменится, менять придётся
только данные.

## Контент

Изображения готовит `tools/ingest.mjs`. Источника два, обработка у них общая:
ужать до 1600px по длинной стороне (с учётом EXIF-поворота), сконвертировать
в WebP, снять blur-плейсхолдер и разложить в `public/catalog/<категория>/`.

- `--web` — страницы старого сайта из `tools/sources.json` → `pages`.
  Ссылки собираются из `src`, `srcset` и ленивых `data-`атрибутов, файлы
  кэшируются в `tools/.cache`.
- `--local` — папки с оригиналами с камеры из `tools/sources.json` → `folders`.
  Папки лежат рядом с репозиторием: это несколько гигабайт съёмки, в git уходит
  только результат обработки.

```bash
NODE_USE_ENV_PROXY=1 npm run ingest -- --web
npm run ingest -- --local --write-content
```

Разбивка по товарам берётся из `tools/photo-plan.json`: там для каждого товара
перечислены его ракурсы, потому что на глаз это видно, а по порядку файлов —
нет. Там же лежит всё, что пишет человек: название, артикул, описание, теги,
ссылка на ролик и отметка `cover` для обложки категории. Без плана скрипт режет
последовательность подряд по `groupSize` и отдаёт черновой
`tools/ingest-manifest.json`, с которым дальше работают руками.

Папка съёмки и раздел каталога — не одно и то же: подсвечники, шкатулки, подносы
и саше снимались вместе со свечами и гипсом. Из какой папки брать файлы, товар
говорит полем `source`; по умолчанию это его же категория. Раскладывается
изделие всегда по своей категории — `public/catalog/<категория>/`.

Флаг `--write-content` переносит готовое в `src/data/products.json` и обложки
в `categories.json`. План здесь источник истины: всё, чего в нём нет, из данных
исчезает, а обложки категорий вне плана обнуляются — иначе частичный прогон
оставил бы на главной ссылку на несуществующий файл. Отсюда правило: правку
руками в `products.json` следующий прогон затрёт, поэтому вносить её нужно
в план. Цену скрипт не пишет никогда, характеристики не выдумывает — они
остаются пустыми до заполнения заказчицей.

Отбирать кадры помогает `npm run metrics`: считает по оригиналам резкость,
перцептивный хеш (точные и почти одинаковые дубли) и ориентацию, складывает
в `tools/.cache/metrics.json` и дописывает только новое. Резкость сравнима
лишь внутри одной съёмки — тёмный фон занижает её у совершенно резкого кадра,
поэтому это подсказка при выборе между соседними дублями, а не приговор.

## Предварительные характеристики

⚠️ Размеры, ароматы, состав и время горения в `products.json` **придуманы
разработчиком** для показа: заказчица их не подтверждала. Живут они в
`tools/draft-content.json`, откуда переносятся в данные. Заполнены только
пятнадцать товаров первой партии; у остальных характеристик нет, и блок
в карточке просто не выводится.

```bash
npm run content:drafts        # проставить предварительные значения
npm run content:clear-drafts  # стереть их обратно
```

До выхода на боевой домен каждую строку нужно либо заменить настоящей, либо
стереть. Цен здесь нет намеренно: заказчица просила убрать их с сайта совсем,
и скрипт `price` не трогает.

Порядок при добавлении товаров: сначала `npm run ingest -- --local
--write-content`, потом `npm run content:drafts` — ingest перезаписывает
`products.json` целиком и характеристики при этом обнуляет.

## Превью для соцсетей

`src/app/opengraph-image.tsx` собирает картинку 1200×630 на этапе сборки:
имя, строка позиционирования и кадр из каталога (`public/og/candle.jpg`).
Шрифты тянутся с Google Fonts в формате truetype — satori не понимает woff2,
а Google отдаёт ttf только клиентам со старым user-agent.

Заменить кадр: положить новый `public/og/candle.jpg` (520×630) и пересобрать.

## Чего в проекте нет

- **Админки.** Контент правится только в `tools/photo-plan.json` и прогоняется
  скриптом. Заказчица сама ничего изменить не может — это отдельная работа.
- **Цен.** Убраны по просьбе заказчицы, `price` везде `null`.
- **Характеристик у большинства товаров.** Размеры и состав есть только
  у первой партии, и те предварительные.

## Куда это едет дальше

Боевой сайт заказчица просила на WordPress — этот репозиторий остаётся
прототипом: эталоном дизайна, структуры и поведения. Что переносится как есть,
что придётся собирать заново и как модель категорий и тегов ложится на
таксономии WordPress — в `docs/wordpress.md`.

## Анимация

Ключевой эффект — расходящиеся половины экрана (`src/components/home/SplitReveal.tsx`).
Источник анимации — прогресс скролла (`useScroll` + `useTransform`), а не `mousemove`,
поэтому эффект одинаково работает на десктопе и на телефоне. Курсорный параллакс
добавляется поверх только на устройствах с точным указателем и ограничен 8px.

Анимируются исключительно `transform` и `opacity`. При `prefers-reduced-motion`
секция становится обычной статической, а Lenis не инициализируется.

## Деплой

Vercel, пресет Next.js, без переменных окружения. Все маршруты пререндерятся
на сборке; `output: "export"` намеренно не включён, чтобы работала оптимизация
`next/image`.
