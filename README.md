# Candles_Soap

Сайт-каталог изделий ручной работы Анны Манасарян: свечи, мыло, гипс, декор.
Прототип для показа заказчику — кликабельный сайт, не макет.

## Стек

Next.js 15 (App Router, TypeScript) · Tailwind CSS v4 · Framer Motion · Lenis · next/image.
CMS нет: контент лежит в `src/data/*.json` и разбирается zod-схемами.

## Запуск

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # продакшен-сборка, все страницы пререндерятся статически
npm run typecheck
```

## Структура

```
src/data/          контент: categories, tags, products, site
src/lib/schemas.ts zod-схемы — единственное описание формы данных
src/lib/content.ts единственная точка чтения контента
src/components/    ui/ — переиспользуемое, home/ — блоки главной
tools/ingest.mjs   обработка изображений: старый сайт или локальные папки
tools/photo-plan.json  разбивка кадров по товарам и тексты карточек
```

### Замена источника данных на REST

Компоненты не импортируют JSON напрямую — только функции из `src/lib/content.ts`.
Чтобы перейти на API, переписывается один этот модуль: схемы и типы остаются,
ни один компонент не меняется.

## Модель данных

- `categories.json` — 8 категорий: `candles`, `soap`, `gypsum`, `sachet`,
  `holders`, `boxes`, `plates`, `decor`.
- `tags.json` — теги двух групп: `occasion` (повод) и `recipient` (адресат).
- `products.json` — у товара ровно одна категория и сколько угодно тегов.

Плоский список из 38 разделов заказчицы раскладывается на эту пару осей:
то, что было отдельным разделом («новогодние свечи», «мыло для мужчин»),
собирается фильтрами категория + теги.

## Контент

Изображения готовит `tools/ingest.mjs`. Источника два, обработка у них общая:
ужать до 1600px по длинной стороне (с учётом EXIF-поворота), сконвертировать
в WebP, снять blur-плейсхолдер и разложить в `public/catalog/<категория>/`.

- `--web` — страницы старого сайта из `tools/sources.json` → `pages`.
  Ссылки собираются из `src`, `srcset` и ленивых `data-`атрибутов, файлы
  кэшируются в `tools/.cache`.
- `--local` — папки с оригиналами с камеры из `tools/sources.json` → `folders`.
  Папки лежат рядом с репозиторием: это несколько гигабайт съёмки, в git уходит
  только результат обработки.

```bash
NODE_USE_ENV_PROXY=1 npm run ingest -- --web
npm run ingest -- --local --write-content
```

Разбивка по товарам берётся из `tools/photo-plan.json`: там для каждого товара
перечислены его ракурсы, потому что на глаз это видно, а по порядку файлов —
нет. Там же лежит всё, что пишет человек: название, описание, теги, отметка
`cover` для обложки категории и портрет для первого экрана. Без плана скрипт
режет последовательность подряд по `groupSize` и отдаёт черновой
`tools/ingest-manifest.json`, с которым дальше работают руками.

Флаг `--write-content` переносит готовое в `src/data/products.json`, обложки
в `categories.json` и портрет в `site.json`. Цены и характеристики скрипт
не выдумывает — они остаются пустыми до заполнения заказчицей.

## Анимация

Ключевой эффект — расходящиеся половины экрана (`src/components/home/SplitReveal.tsx`).
Источник анимации — прогресс скролла (`useScroll` + `useTransform`), а не `mousemove`,
поэтому эффект одинаково работает на десктопе и на телефоне. Курсорный параллакс
добавляется поверх только на устройствах с точным указателем и ограничен 8px.

Анимируются исключительно `transform` и `opacity`. При `prefers-reduced-motion`
секция становится обычной статической, а Lenis не инициализируется.

## Деплой

Vercel, пресет Next.js, без переменных окружения. Все маршруты пререндерятся
на сборке; `output: "export"` намеренно не включён, чтобы работала оптимизация
`next/image`.
