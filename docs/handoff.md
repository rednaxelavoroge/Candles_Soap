# Candles_Soap — передача следующей сессии

Сайт для Анны Манасарян: свечи, мыло, гипс, декор ручной работы.
Прототип живой: **https://candles-soap.vercel.app** — деплой из `main`.
Репозиторий `rednaxelavoroge/Candles_Soap`, актуальный коммит на момент передачи `d9b168e`.

Локальный клон у заказчика лежит **не** там, где кажется:
`~/Desktop/Projectrs/Candles_Soap/originals/Candles_Soap` (внутри папки `originals`,
рядом с папками съёмки). В `~/Desktop/Projectrs/Candles_Soap` git-репозитория нет.

## Стек

Next.js 15 App Router, Tailwind v4, Framer Motion, Lenis. Весь контент в
`src/data/*.json` под zod-схемами (`src/lib/schemas.ts`), компоненты читают его
только через `src/lib/content.ts`. Шрифт — Comfortaa (тот же, что на её нынешнем
сайте annamanasaryan.com).

Команды: `dev`, `build`, `typecheck`, `ingest`, `metrics`, `content:drafts`,
`content:clear-drafts`, `find-videos`, `portrait`, `tones`.

## ЖЁСТКИЕ ПРАВИЛА ЗАКАЗЧИЦЫ — НЕ НАРУШАТЬ

1. **Цен нигде нет.** Ни в сетке, ни в карточке. `price` всегда `null`.
2. **Артикул у каждого изделия**: СВ свечи, МЛ мыло, ГП гипс, СШ саше,
   ПС подсвечники, ШК шкатулки, ПД блюда, ДК декор.
3. **Чёрного фона не хочет.** Всё в бежевой гамме, палитра в `globals.css`.
   Затемнение при наведении идёт тоном `--ink` с прозрачностью, не чистым чёрным.
4. **Сайт — галерея, а не магазин.** В сетке только фотографии, без подписей.
5. **Белая рамка между фотографиями всегда.**
6. **При наведении кадр темнеет**, на нём проступают название и артикул.
   Её слова: «люди часто не хотят открывать, увидели и название выписали».
   Лайки не нужны — она сказала это прямо.
7. **Никаких теней и градиентов.**
8. **Скруглённые края у кадров** — она попросила это позже, прежнее правило
   про строгие прямые углы отменено ею же.

## Что сделано в этой сессии

### По референсам, со снятием живых стилей

Замерено в браузере на её референсах, не по описанию:

- azalea/portfolio-gallery: плашка при наведении `rgba(255,255,255,.9)`,
  во весь кадр, центр по обеим осям, `0.4s ease`. Структуру взяли, но потом
  заменили на затемнение — её словесная просьба оказалась точнее референса.
- themegoods/photography demo4: наезд `scale(1.3)`, `0.3s ease-in-out`.
  У нас 1.18 / 500ms, правится переменной `--tile-zoom` в `globals.css`.
- ascandle.ru: фон `#F3ECE2`, приглушённый текст `#91766E`, розово-бежевый
  `#C8A19C`. Наша палитра чуть светлее, менять не стали.

### Правки по её замечаниям

- **Шрифт** Comfortaa вместо Cormorant + Jost («явно не мой шрифт»).
- **Расходящиеся половины** на главной (`SplitReveal`) разъезжаются влево-вправо
  по диагонали, а не вверх-вниз. Левое слово поднято, правое опущено.
- **Плитка**: затемнение во весь кадр при наведении + название + артикул.
  На тач-устройствах и у витринных плиток — полоса по нижнему краю, иначе
  сетка потемнела бы целиком и навсегда.
- **Кладка изделий** (`ProductGrid`): колонками, каждый кадр в своих пропорциях.
  Раньше был шаблон из шести слотов, который **обрезал** снимки под чужую форму.
  В данных 9 горизонтальных обложек, 22 квадратных, 4 вертикальных.
- **Каталог макросекциями** (`CatalogSections`): раздел во весь экран, половина
  текст / половина кадр, стороны чередуются, нумерация `01 / 08`.
  Секция закреплена (`h-[200svh]` + `sticky`), половины приходят из-за краёв
  экрана, сходятся, держатся и расходятся. Без закрепления движение было,
  но смазывалось за долю секунды — заказчица прочла это как «ничего не сделано».
- **Акварель вокруг кадра** (`Watercolor`): пятна с рваным мягким краем плюс
  капли и колечки. Цвет **берётся из самой фотографии** командой `npm run tones`.
  Стоит на первом экране и в карточке товара.
- **Кляксы** (`Blots`) в фонах секций перерисованы в мазки кистью и убраны
  в поля — она жаловалась, что они налезают на шрифт.
- Убрано по её просьбе: полоса «Готово к отправке», секция «Как это работает»,
  форма заявки в контактах. «Направления» переименованы в «Каталог».
- Портрет Анны на первом экране (`npm run portrait -- <файл>`).

## Что НЕ сделано

### 1. Разделы второго уровня — главное незакрытое

Она хочет три уровня: **Каталог → Разделы → Фотографии**.
Нажал «Мыло» — открылись Морская тема, Мужчинам, Детям; нажал раздел — матрица
фотографий. Сейчас нажатие на макросекцию ведёт **сразу** на страницу раздела
с фильтрами-кнопками «Повод / Кому / Форма».

Её черновик (сделан её сыном) выглядит так: экран пополам, слева `01 / 04`,
название, описание, «Смотреть изделия →», справа кадр; половины разъезжаются
горизонтально, следующий раздел въезжает следом. Ссылку на артефакт открыть
не удалось (расшарен публично, чтение таких артефактов недоступно) — есть
только скриншоты в переписке.

**Упирается не в код, а в наполнение.** Сейчас: 35 изделий, **10 без единого
тега**, почти в каждом разделе по одному изделию. В её брифе для одних свечей
четырнадцать разделов. Если сделать экран разделов сейчас, она увидит витрину,
где половина полок пустая. Сначала имеет смысл добрать товары.

### 2. Добор товаров

6 ГБ оригиналов у заказчицы на Маке: `~/Desktop/Projectrs/Candles_Soap/originals`
— папки `1-СВЕЧИ`, `1-МЫЛО`, `1-ГИПС`, 708 фото и 52 видео. **Облачная сессия
их не видит**, добор делается локальной сессией Claude Code из папки проекта.
Конвейер: правится `tools/photo-plan.json`, затем
`npm run ingest -- --local --write-content`, потом `npm run tones`.

### 3. Прочее

- Акварель стоит только на первом экране и в карточке товара; в шапках разделов
  и на бэкстейдже её нет.
- Бэкстейдж (`/backstage`) остался на прежней сетке — она просила несимметричную
  матрицу «горизонт вместе с вертикальными» и там тоже.
- Похожие изделия подбираются по категории. Она спрашивала, по какому принципу,
  и хотела бы выбирать сама — поля для ручного выбора нет.
- Админки нет и не планировалось: это дизайн-эталон, боевой сайт будет на
  WordPress (`docs/wordpress.md`). **Она просила админку с первого сообщения** —
  если спросит «где я это редактирую», ответ должен быть готов.

## Ждём от заказчицы

- Настоящие характеристики: у 15 изделий они **придуманы разработчиком**,
  лежат в `tools/draft-content.json`, снимаются `npm run content:clear-drafts`.
  До боевого домена заменить или стереть.
- Тексты разделов каталога (в `CatalogSections` готово поле `intro`, пока `null`,
  вместо него выводится количество изделий). Описание её изделий сочинять нельзя —
  это утверждения о настоящем товаре.
- Ссылку на Facebook (`site.json` → `contacts.facebook`, сейчас `null`).
- Ссылки на её ролики в YouTube. Сейчас в карточках 3 демонстрационных ролика,
  собранных из её же фотографий через ffmpeg. Карточка умеет lite-embed:
  плеер грузится только по клику.

## Грабли этого окружения — прочитать до работы

1. **Vercel не собирает ветки.** Пуши в `claude/*` он пропускает через раз, а то
   и вовсе. В `main` за всю историю проекта собралось всё до единого пуша.
   Работать через `main`. Кнопка **Redeploy** в панели пересобирает **тот же**
   коммит, а не свежий — она не помогает.
2. **Никогда не обрезать вывод скриптов через `head`.** Скрипты пишут файл в
   конце; `head` закрывает пайп, процесс убивается сигналом **до записи**.
   На этом дважды потерялись результаты (сборка и `npm run tones`).
   Писать в файл: `npm run tones > /tmp/log 2>&1`.
3. **Прокси может блокировать `vercel.app`** (403 на CONNECT). Тогда `curl`
   отдаёт пустоту, а `grep -c` по пустоте даёт `0` — это легко принять за
   «изменения не доехали». Проверять через MCP-инструмент
   `mcp__Vercel__web_fetch_vercel_url`, а не курлом.
4. **Chromium через прокси требует `--ssl-version-max=tls1.2`** — апстрим
   сбрасывает TLS 1.3 ClientHello сразу после `CONNECT 200`. Проверку
   сертификатов не отключать. Прокси-адрес брать из `$HTTPS_PROXY`, он меняется.
5. **Проверять результат фактически, а не по факту пуша.** Дважды выходило так,
   что «запушено» ≠ «видно на сайте», и заказчик видел старую версию.

## Референсы заказчицы

- https://azalea.qodeinteractive.com/split-slider-showcase/ — кляксы
- https://azalea.qodeinteractive.com/portfolio-gallery/ — подпись при наведении
- https://themes.themegoods.com/photography/demo4/home/home-5-photographer-2/ — наезд и белые рамки
- https://ascandle.ru/interior-candles/tproduct/460228315862-svecha-bereza-3-sht — бежевая гамма
- https://promo-theme.com/novo/split-screen/ — анимация, которая ей нравится.
  Её главная боль: там она не работает на телефоне. **У нас решено** — половины
  завязаны на прокрутку, а не на мышь. Проверено на 390×664, переделывать не нужно.
- Для будущего фотосайта, не для свечей: core-minimalist-photography-portfolio
  и sansara на themeforest (обе за Cloudflare, не открываются).

## Контакты заказчицы (уже в site.json)

WhatsApp +374 98 033 550 — на него кнопка. Телефон +7 915 003 35 50 — без кнопки.
Instagram annamanasaryan.art. Facebook ждём. Домен annamanasaryan.art существует,
там её нынешний сайт.
